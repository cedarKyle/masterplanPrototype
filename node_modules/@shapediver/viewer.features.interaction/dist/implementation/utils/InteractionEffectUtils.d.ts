import { ITreeNode } from "@shapediver/viewer.shared.node-tree";
import { IMaterialAbstractData } from "@shapediver/viewer.shared.types";
import { IInteractionEffectUtils } from "../../interfaces/utils/IInteractionEffectUtils";
export declare class InteractionEffectUtils implements IInteractionEffectUtils {
    #private;
    /**
     * Apply the effect material to the node and all descendents.
     *
     * @param node
     * @param material
     * @returns
     */
    applyEffectMaterial(node: ITreeNode, material: IMaterialAbstractData): string;
    /**
     * Remove the effect material with the specified token from the node and all descendents.
     *
     * @param node
     * @param token
     */
    removeEffectMaterial(node: ITreeNode, token: string): void;
}
//# sourceMappingURL=InteractionEffectUtils.d.ts.map