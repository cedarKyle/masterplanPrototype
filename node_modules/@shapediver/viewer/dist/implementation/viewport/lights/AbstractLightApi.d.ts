import { vec3 } from "gl-matrix";
import { ILight, LIGHT_TYPE } from "@shapediver/viewer.rendering-engine.light-engine";
import { ILightApi } from "../../../interfaces/viewport/lights/ILightApi";
import { IViewportApi } from "../../../interfaces/viewport/IViewportApi";
export declare abstract class AbstractLightApi implements ILightApi {
    #private;
    protected scope: string;
    constructor(viewportApi: IViewportApi, light: ILight);
    get color(): string | number | vec3;
    set color(value: string | number | vec3);
    get id(): string;
    get intensity(): number;
    set intensity(value: number);
    get name(): string | undefined;
    set name(value: string | undefined);
    get order(): number | undefined;
    set order(value: number | undefined);
    get type(): LIGHT_TYPE;
}
//# sourceMappingURL=AbstractLightApi.d.ts.map