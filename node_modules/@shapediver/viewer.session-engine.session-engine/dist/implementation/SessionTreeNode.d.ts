import { ITransformation, ITreeNode, ITreeNodeData, TreeNode } from '@shapediver/viewer.shared.node-tree';
import { ISessionTreeNode } from '../interfaces/ISessionTreeNode';
export declare class SessionTreeNode extends TreeNode implements ISessionTreeNode {
    #private;
    /**
     * Special scene graph node for session data. Only to be created internally.
     *
     * @param name the name of the node
     * @param parent the parent of this node
     * @param data the array of data
     * @param transformation the array of transformations
     */
    constructor(name?: string, parent?: ITreeNode, data?: ITreeNodeData[], transformations?: ITransformation[]);
    get sessionNode(): boolean;
}
//# sourceMappingURL=SessionTreeNode.d.ts.map